var familia = ['Ale', 'Cesi', 'Mar', 'Any'];
var familiaDos = ['sabri', 'Ani'];

// length Devuelve la cantidad de elementos del arreglo

var cantidadElementosFamilia = familia.length;

// concat permite unir arreglos

var unionfamilias = familia.concat(familiaDos);

// join contrario a split muestra todos los elementos del arreglo en una cadena de texto

var texto = familia.join('-');

// pop Elimina el último elemento del arreglo

// var sinUno = familia.pop();

// push agrega un elemento al final del arreglo

// familia.push('Niño');

// shift Elimina el primer elemento del arreglo

// familia.shift();

// unshift Agrega un elemento al principio del arreglo

familia.unshift('Epa');

// reverse ordena el arreglo al revés

familia.reverse();